*** Begin Patch
*** Update File: frontend/style.css
@@
 .kanban-board {
   display: grid;
   grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
   gap: var(--space-24);
   margin-top: var(--space-24);
 }
 
 .kanban-column {
   background-color: var(--color-bg-2);
   border-radius: var(--radius-lg);
   padding: var(--space-16);
   min-height: 400px;
 }
@@
 .task-card p {
   color: var(--color-text-secondary);
   font-size: var(--font-size-sm);
   margin-bottom: var(--space-12);
 }
+
+/* Status-aware Kanban accents (match reference screenshot) */
+/* Column tints */
+.kanban-column[data-status="pending"] { background: rgba(var(--color-teal-500-rgb), 0.06); }
+.kanban-column[data-status="in-progress"] { background: rgba(var(--color-orange-500-rgb), 0.06); }
+.kanban-column[data-status="completed"] { background: rgba(var(--color-red-500-rgb), 0.06); }
+
+/* Card left borders inside each column */
+.kanban-column[data-status="pending"] .task-card { border-left: 6px solid var(--color-primary); }
+.kanban-column[data-status="in-progress"] .task-card { border-left: 6px solid var(--color-orange-500); }
+.kanban-column[data-status="completed"] .task-card { border-left: 6px solid var(--color-red-500); }
+
+/* Column headings subtle underline */
+.kanban-column[data-status] h3 { border-bottom: 1px solid rgba(255,255,255,0.08); padding-bottom: 8px; }
*** End Patch
