# Frontend environment variables
# Point this to the backend base URL in production (defaults to http://localhost:4000 in dev)
VITE_API_BASE_URL=http://localhost:4000
# Optional: mirror the backend upload limit to inform users
VITE_MAX_UPLOAD_SIZE_MB=500
